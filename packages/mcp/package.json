{
  "name": "@codingverse/mcp",
  "version": "0.0.0",
  "description": "codingverse MCP server — 7 tools (search/pack/expand/get_file/callers/callees/impact) over stdio",
  "type": "module",
  "bin": {
    "cv-mcp": "./dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node ./dist/bin.js"
  },
  "dependencies": {
    "@codingverse/core": "workspace:*",
    "@codingverse/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  },
  "license": "MIT"
}
